import { TicketsView } from "@/components/TicketsView";

export const metadata = {
  title: "Tickets - Roqueta",
  description: "Gestión interna de tickets de soporte de Formeta.",
};

export default function TicketsPage() {
  return <TicketsView />;
}
